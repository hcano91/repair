import { ProvideConsultationFormDirective } from './provide-consultation-form.directive';

describe('ProvideConsultationFormDirective', () => {
  it('should create an instance', () => {
    const directive = new ProvideConsultationFormDirective();
    expect(directive).toBeTruthy();
  });
});
